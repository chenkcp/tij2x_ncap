// This layer handles:

// table-specific queries
// insert/update/select SQL
// returning data for one business area

// It should not know environment variables, hostnames, or how site config is loaded.